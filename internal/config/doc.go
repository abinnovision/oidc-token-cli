// Package config parses CLI flags and optional config-file settings that
// describe an OIDC issuer, public client, and token request (issuer,
// client-id, scope, audience, grant-type, token-type, token-store-dir, redirect).
package config
